apiVersion: v1
kind: Service

metadata:
  name: northwind-delivery-service

spec:
  type: NodePort

  selector:
    app: northwind-delivery-service

  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30080